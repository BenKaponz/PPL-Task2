(define bind
    (lambda (v f)
        (if (is-error? v)
            v
            (f v)
        )
    )
)

(define make-error
    (lambda () 'error)
)

(define is-error?
    (lambda (x)
        (eq? x 'error)
    )
)

(define isDict?
    (lambda (d)
        (if (eq? d '())
            #t
            (if (pair? d)
                (if (pair? (car d))
                    (if (symbol? (car (car d)))
                        (isDict? (cdr d))
                        #f
                    )
                    #f
                )
                #f
            )
        )
    )
)

(define no-duplicates?
    (lambda (d)
        (if (eq? d '())
            #f
            (if (is-error? (get (cdr d) (car (car d))))
                (no-duplicates? (cdr d))
                #f
            )
        )
    ) 
)

(define dict?
    (lambda (d)
        (if (isDict? d)
            (no-duplicates? d)
            #f
        )
    )
)

(define dict
    (lambda (p)
        (if (dict? p)
            p
            (make-error)
        )
    )
)

(define get
    (lambda (d key)
        (if (dict? d)
            (if (eq? (car (car d)) key)
                (cdr (car d))
                (if (eq? (cdr d) '())
                    (make-error)
                    (get (cdr d) key)
                )
            )
            (make-error)
        )
    )
)
